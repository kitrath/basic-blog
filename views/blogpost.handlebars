<div class="row justify-content-center my-2">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">{{ title }}</h3>
                <h6 class="card-subtitle mb-2 text-muted">by {{ user.name }}, {{format_date createdAt}}</h5>
                <p class="card-text">{{ content }}...</p>
            </div>
        </div>
    </div>
</div>